{
  "name": "consensio-frontend",
  "private": true,
  "type": "module",
  "description": "Build and test toolchain for the classic-script frontend in static/.",
  "scripts": {
    "build": "node scripts/build_frontend.mjs",
    "build:check": "node scripts/build_frontend.mjs --check",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "dompurify": "3.0.6",
    "esbuild": "^0.21.5",
    "jsdom": "^24.1.0",
    "katex": "0.17.0",
    "marked": "12.0.2",
    "vitest": "^1.6.0"
  }
}
